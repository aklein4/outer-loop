defaults:
  - forte-llama3p2-1b
  - _self_


# This checkpoint was created using scripts/convert_hf_checkpoint.py
pretrained_url: aklein4/forte-init
pretrained_step: 0
# The port adds controller biases and removes up_fast biases. Older forte-init
# checkpoints therefore need non-strict loading until a new init is published.
pretrained_strict: false
